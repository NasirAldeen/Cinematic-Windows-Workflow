# Soundtrack assets

Place your own licensed startup soundtrack in this directory and update
`SoundFile` and `IntroDurationSeconds` in `config.psd1`.

The default expected filename is `startup-sound.mp3`. If it is missing, the
intro generates and uses a small original synthesized fallback sound.

Audio files are ignored by Git so personal or copyrighted recordings are not
accidentally published.
